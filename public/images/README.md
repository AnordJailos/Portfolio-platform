Put your real images here:
- avatar.jpg — your photo (referenced by SITE.avatarUrl in lib/constants.ts)
- og-default.jpg — 1200x630 social share image (referenced by SEO_DEFAULTS.ogImage)
- any project cover images / gallery images you reference directly instead of uploading via /admin

This file itself is just a placeholder so the empty directory has something in it — delete it once you've added real images.
